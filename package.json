{
  "name": "cf-proxy-relay",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.19",
    "vitest": "3.2.0",
    "wrangler": "4.51.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "deploy": "wrangler deploy"
  }
}
